<% layout('layouts/boilerplate.ejs') -%>
<div class="row">
    <div class="alert alert-danger col-6 offset-3" role="alert" > 
        <h4 class="alert-heading">Error!</h4>
        <p><strong><%= err.statusCode || 500 %></strong></p>
        <p><%= err.message || "Something went wrong!" %></p>
    </div>
</div>